#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Get current branch name
current_branch=$(git symbolic-ref --short HEAD)
echo "📎 Current branch: $current_branch"

# Function to get PR body
get_pr_body() {
    default_body=$1
    echo "💬 Enter PR body (press CTRL+D when finished, or ENTER to use default):"
    echo "Default body: $default_body"
    echo "-------------------"
    # Try to read user input
    NEW_BODY=$(cat)
    
    # If nothing was typed, use default body
    if [ -z "$NEW_BODY" ]; then
        echo "$default_body"
    else
        echo "$NEW_BODY"
    fi
}

case "$current_branch" in
  feature/*)
    PR_TITLE="Feature completed: ${current_branch#feature/}"
    DEFAULT_BODY="This feature is ready for review."
    PR_BODY=$(get_pr_body "$DEFAULT_BODY")
    BASE_BRANCH="dev"
    ;;
  hotfix/*)
    # Create PR for main
    PR_TITLE="Hotfix: ${current_branch#hotfix/}"
    DEFAULT_BODY="Urgent hotfix for production."
    PR_BODY=$(get_pr_body "$DEFAULT_BODY")
    
    echo "📝 Creating Pull Request for main..."
    gh pr create \
      --title "$PR_TITLE" \
      --body "$PR_BODY" \
      --base "main" \
      --head "$current_branch"
    
    # Create PR for dev
    echo "📝 Creating Pull Request for dev..."
    gh pr create \
      --title "$PR_TITLE" \
      --body "$PR_BODY" \
      --base "dev" \
      --head "$current_branch"
    
    exit 0
    ;;
  bugfix/*)
    PR_TITLE="Bugfix: ${current_branch#bugfix/}"
    DEFAULT_BODY="Bug fixed and ready for review."
    PR_BODY=$(get_pr_body "$DEFAULT_BODY")
    BASE_BRANCH="dev"
    ;;
  release/*)
    # Create PR for main
    PR_TITLE="Release: ${current_branch#release/}"
    DEFAULT_BODY="Release is ready for production."
    PR_BODY=$(get_pr_body "$DEFAULT_BODY")
    
    echo "📝 Creating Pull Request for main..."
    gh pr create \
      --title "$PR_TITLE" \
      --body "$PR_BODY" \
      --base "main" \
      --head "$current_branch"
    
    # Create PR for dev
    echo "📝 Creating Pull Request for dev..."
    gh pr create \
      --title "$PR_TITLE" \
      --body "$PR_BODY" \
      --base "dev" \
      --head "$current_branch"
    
    exit 0
    ;;
  *)
    echo "⚠️ Branch $current_branch does not match any configured case. Ignoring."
    exit 0
    ;;
esac

# For feature and bugfix branches that only need one PR
if [[ "$current_branch" =~ ^(feature|bugfix)/ ]]; then
    echo "📝 Creating Pull Request..."
    echo "Title: $PR_TITLE"
    echo "Body: $PR_BODY"
    echo "Base branch: $BASE_BRANCH"
    echo "Source branch: $current_branch"

    gh pr create \
      --title "$PR_TITLE" \
      --body "$PR_BODY" \
      --base "$BASE_BRANCH" \
      --head "$current_branch" || true
fi
[gitflow "branch"]
    master = main
    develop = dev

[gitflow "prefix"]
    feature = feature/
    bugfix = bugfix/
    release = release/
    hotfix = hotfix/
    support = support/
    versiontag = v

[gitflow "feature.finish"]
    hooks = post-flow-finish

[gitflow "hotfix.finish"]
    hooks = post-flow-finish

[gitflow "release.finish"]
    hooks = post-flow-finish

[gitflow "bugfix.finish"]
    hooks = post-flow-finish

[gitflow "path"]
    hooks = .husky